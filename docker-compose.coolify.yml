services:
  birthday-fashion-party:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "3000"
    volumes:
      - rsvp-data:/data
    restart: unless-stopped

volumes:
  rsvp-data:
